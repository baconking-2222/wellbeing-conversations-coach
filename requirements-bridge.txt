# Used by Hugging Face Spaces (Docker template).
# Everything the FastAPI bridge needs to mint Gemini sessions and score.
fastapi>=0.110
uvicorn[standard]>=0.30
httpx>=0.27
python-dotenv>=1.0
anthropic>=0.40
google-genai>=1.0
